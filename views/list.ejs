<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="css/styles.css" />
  <title>To do List</title>
</head>

<body>
  <div class="box" id="heading">
    <h1>
      <%= today %>
    </h1>
  </div>
  <div class="box">
    <% for(var items of item){ %>
      <div class="item">
        <form action="/delete" method="post">
          <div class="item">
            <input value="<%= items._id %>" onchange="this.form.submit()" name="check" type="checkbox" />
            <p><%= items.text %></p>
          </div>
          <input type="hidden" name="listName" value="<%= today %>">
        </form>
      </div>
      <% } %>
        <form class="item" action="/" method="post">
          <input type="text" name="newItem" id="newItem" placeholder="New Item" autocomplete="off" />
          <button type="submit" value="<%= today %>" name="button">+</button>
        </form>
  </div>
</body>

</html>